// auth.js — authentication helpers

/**
 * Check whether a request carries a valid authentication token.
 * This is a stub. Real token validation is not yet implemented.
 * @param {string} token
 * @returns {boolean}
 */
function checkAuth(token) {
  return false;
}

module.exports = { checkAuth };
