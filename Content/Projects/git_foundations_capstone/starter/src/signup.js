// signup.js — newsletter signup validation

/**
 * Validate a newsletter signup email address.
 * @param {string} email
 * @returns {boolean}
 */
function validateSignup(email) {
  // TODO: add email validation
  return true;
}

module.exports = { validateSignup };
