# Exercise 3 — Git Basics

Initialise a repository and make your first commit:

```bash
cd ~/exercises/03-git
git init
git status
echo '# My Project' > README.md
git add README.md
git commit -m 'Initial commit'
git log
```
